import * as commands from '../consts/commands.const';

export enum CommandsEnum {
  BACKSPACE = commands.BACKSPACE,
  TAB = commands.TAB,
  ENTER = commands.ENTER,
  SHIFT = commands.SHIFT,
  CTRL = commands.CTRL,
  ALT = commands.ALT,
  PAUSE = commands.PAUSE,
  CAPSLOCK = commands.CAPSLOCK,
  ESCAPE = commands.ESCAPE,
  SPACE = commands.SPACE,
}
