import * as numpad from '../consts/numpad.const';

export enum NumpadEnum {

    NUMPAD_0 = numpad.NUMPAD_0,
    NUMPAD_1 = numpad.NUMPAD_1,
    NUMPAD_2 = numpad.NUMPAD_2,
    NUMPAD_3 = numpad.NUMPAD_3,
    NUMPAD_4 = numpad.NUMPAD_4,
    NUMPAD_5 = numpad.NUMPAD_5,
    NUMPAD_6 = numpad.NUMPAD_6,
    NUMPAD_7 = numpad.NUMPAD_7,
    NUMPAD_8 = numpad.NUMPAD_8,
    NUMPAD_9 = numpad.NUMPAD_9,
}
