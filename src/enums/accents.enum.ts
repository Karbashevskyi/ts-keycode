import {accents} from "../consts/accents.const";

export enum AccentsEnum {
    NUM_LOCK = accents.NUM_LOCK,
    SCROLL_LOCK = accents.SCROLL_LOCK,
    SEMICOLON = accents.SEMICOLON,
    EQUAL_SIGN = accents.EQUAL_SIGN,
    COMMA = accents.COMMA,
    DASH = accents.DASH,
    PERIOD = accents.PERIOD,
    FORWARD_SLASH = accents.FORWARD_SLASH,
    GRAVE_ACCENT = accents.GRAVE_ACCENT,
    OPEN_BRACKET = accents.OPEN_BRACKET,
    BACKSLASH = accents.BACKSLASH,
    CLOSE_BRACKET = accents.CLOSE_BRACKET,
    SINGLE_QUOTE = accents.SINGLE_QUOTE,
}
