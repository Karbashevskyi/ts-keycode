import * as functions from '../consts/functions.const';

export enum FunctionsEnum {

    PAGEUP = functions.PAGEUP,
    PAGEDOWN = functions.PAGEDOWN,
    END = functions.END,
    HOME = functions.HOME,
    LEFT = functions.LEFT,
    UP = functions.UP,
    RIGHT = functions.RIGHT,
    DOWN = functions.DOWN,
    INSERT = functions.INSERT,
    DELETE = functions.DELETE,
}
