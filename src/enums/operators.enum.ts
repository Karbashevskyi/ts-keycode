import * as operators from '../consts/operators.const';

export enum OperatorsEnum {

    MULTIPLY = operators.MULTIPLY,
    ADD = operators.ADD,
    SUBTRACT = operators.SUBTRACT,
    DECIMAL_POINT = operators.DECIMAL_POINT,
    DIVIDE = operators.DIVIDE,
}
